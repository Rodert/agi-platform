import { FileImageOutlined, FontColorsOutlined, NodeIndexOutlined } from '@ant-design/icons'
import { Button, Input, Modal, Upload, message } from 'antd'
import { useState } from 'react'
const tools=[['图片反推提示词','上传图片，AI 自动反推生成提示词',FontColorsOutlined],['图片转 SVG','将位图转换为可缩放的矢量图形',NodeIndexOutlined],['图片格式转换','转换并压缩 PNG、JPG、WebP 图片',FileImageOutlined]] as const
export function ToolsPage(){const [active,setActive]=useState<(typeof tools)[number]|null>(null),[msg,ctx]=message.useMessage();return <main className="page">{ctx}<h1 className="page-title">工具箱</h1><p className="page-subtitle">实用的 AI 创作辅助工具</p><div className="mt-7 grid grid-cols-3 gap-4 max-[850px]:grid-cols-2 max-[520px]:grid-cols-1">{tools.map(t=>{const Icon=t[2];return <button key={t[0]} onClick={()=>setActive(t)} className="surface tool-card p-5 text-left transition hover:!border-[#3b777a]"><span className="grid h-11 w-11 place-items-center rounded-xl bg-[#1b2931] text-xl text-[#54d6cf]"><Icon/></span><span><b className="block text-base">{t[0]}</b><small className="mt-1 block text-[#8d96a8]">{t[1]}</small></span></button>})}</div><p className="mt-8 text-center text-[#68687b]">更多工具即将推出</p><Modal open={!!active} title={active?.[0]} onCancel={()=>setActive(null)} footer={null}><p className="text-[#8f98aa]">{active?.[1]}</p><Upload.Dragger beforeUpload={()=>false} showUploadList={false}><p className="text-3xl">＋</p><p>点击或拖入图片</p></Upload.Dragger>{active?.[0]==='图片反推提示词'&&<Input.TextArea className="mt-4" rows={4} placeholder="解析后的提示词将在这里显示"/>}<Button block type="primary" className="!mt-4" onClick={()=>msg.success('模拟处理已完成')}>开始处理</Button></Modal></main>}
