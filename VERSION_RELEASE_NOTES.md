## 新增功能

- 新增 `DEVELOP.md`，统一说明本地源码调试、服务器部署和镜像更新流程。
- 支持通过环境变量配置首次创建的超级管理员账号、密码和显示名称。

## 优化改进

- 生产部署要求显式配置超级管理员凭据；本地调试可通过 `LOCAL_SUPER_ADMIN_*` 覆盖。
- 完善启动脚本、管理端说明和部署文档中的访问地址与管理员配置指引。

## Bug 修复

- 移除种子数据和登录页中固定的 `admin / admin123` 凭据，避免共享默认账号风险。
- 已存在的超级管理员不会因修改环境变量而被重置。
