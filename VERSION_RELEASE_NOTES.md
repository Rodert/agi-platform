## 新增功能

- 生成页历史记录支持手动加载更多。
- 分享作品支持将作品海报保存为 PNG 到本地。
- 图片和视频任务使用独立队列与独立 Worker 并发配置，后台可设置图片、视频并发数。
- 管理端生成记录增加任务提交、最终结束及每次执行的开始、结束时间。

## 优化改进

- Worker 重启时会恢复排队任务和已有上游任务 ID 的轮询任务。

## Bug 修复

- 修复非幂等上游在请求中断或 Worker 重启时可能重复提交生成的问题；提交结果未知的任务将失败并自动退款。
- 修复重复队列消息可能导致同一任务重复执行的问题。
