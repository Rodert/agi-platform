## 新增功能

- 发布更新会自动执行镜像中尚未应用的数据库迁移，失败时保留当前应用版本。
- `grok-image-video` 同步时可自动修复历史图片类型记录，无需先手工执行迁移。

## 优化改进

- 数据库保存迁移执行记录，避免应用重启或重复更新时重复执行 SQL。
- 现有部署升级时将 `001-022` 标记为历史基线，并自动补跑 `023-025` 增量迁移。

## Bug 修复

- 修复因未执行 Grok 类型修复迁移而导致渠道模型同步失败的问题。
