## 新增功能

- 生成完成后展示资源 7 天保留提醒与立即下载入口。
- Worker 每小时自动清理过期的图片、视频和缩略图；已发布作品不受影响。

## 优化改进

- 任务页统一提示下载保存，并通过受鉴权接口下载生成结果。
- 本地存储资源统一返回同域 `/uploads/` 路径；AI 读取参考资源时使用 `PUBLIC_BASE_URL` 公网地址。
- Docker 构建排除子目录 `.env`，避免开发环境 API 地址进入生产镜像。

## Bug 修复

- 修复生产站点前端 API 被错误构建为 `http://localhost:8080` 的问题。
- 修复历史本地资源 URL 指向 localhost 的问题。
